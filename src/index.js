import React from 'react';
import ReactDOM from 'react-dom';
import { App } from './Modules/App/App';
import './index.css';

// Render the React component to the #root DOM element.
ReactDOM.render(
  <App />,
  document.getElementById('root')
);
